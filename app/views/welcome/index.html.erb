
<p>
  <% if user_signed_in? %>
    <%= "#{current_user.role.to_s}'s email: " + current_user.email %>
  <% end %>
</p>

<% if user_signed_in? %>
    <h1><%= "Welcome " + current_user.role + "!" %></h1>
<% end %>
